import { BeerListRenderObserverDirective } from './beer-list-render-observer.directive';

describe('BeerListRenderObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new BeerListRenderObserverDirective();
    expect(directive).toBeTruthy();
  });
});
